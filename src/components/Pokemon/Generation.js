import React, { useState} from 'react';
import Pokemon from './Pokemon';
// '../../img/'
const Generation = () =>{
    const imageNames = [1,2,3,4,5]
    return (
        <div>
            {apiValue['types'].map((type, i) => }
        </div>
    )
}

export default Generation;