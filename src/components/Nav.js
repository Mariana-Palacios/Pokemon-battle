import { SiPokemon } from "react-icons/si";

const Nav = (props) => {
    // const {name, image, number} = props;
    return (
      <nav className="nav">
        <SiPokemon className="icon"/>
      </nav>
    );
  }
  
export default Nav;